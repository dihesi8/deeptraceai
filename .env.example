# Copy this to .env.local for local dev. Never commit the real .env.local.

# RPC endpoint for Robinhood Chain. Public RPC works for light read traffic;
# get a free Alchemy/Infura endpoint if you need higher throughput.
RPC_URL=https://rpc.mainnet.chain.robinhood.com
